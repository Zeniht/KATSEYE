import FloatingButton from '../FloatingButton';

export default function FloatingButtonExample() {
  return (
    <div className="relative h-32">
      <p className="font-comic text-sm text-muted-foreground p-4">
        Floating buttons appear fixed on the viewport (check bottom-right in real app)
      </p>
    </div>
  );
}
